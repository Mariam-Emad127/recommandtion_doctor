import 'package:flutter/material.dart';

class Buildstepline extends StatelessWidget {
  const Buildstepline({super.key});

  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Container(
        height: 2,
        color: Colors.grey[300],
      ),
    );
  }
}